<h1>Myers Briggs Personality Test</h1> <br>

<p> For each of the following scales choose a placement that best describes you</p><br>

<%= form_for @score do |f| %>

  <% Score.questions.each_with_index do |q, index| %>
    <%= q[0] + "  " %>
    <% (1..5).each do |n| %>
      <% if n == 3 %>
        <%= radio_button_tag("answers[#{index}]", n, checked: true) %>
      <% else %>
        <%= radio_button_tag("answers[#{index}]", n) %>
      <% end %>
    <% end %>
    <%= q[1] %>
    <hr>
  <%end%>
  <%= submit_tag "Submit" %>
<% end %>